# HR
A command line tool for exporting system user info to console, json or csv files.

# How to use
To install from source, `pip install -e .` from the top-level directory of this project.

`hr --help` provides further instructions